const usuarioObjeto = {
    nome: "João",
    idade: 25
};

function exibirUser(usuarioObjeto) {
    console.log(`${usuarioObjeto.nome } tem ${  usuarioObjeto.idade  } anos.`);
}

exibirUser(usuarioObjeto);
